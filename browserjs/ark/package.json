{
  "name": "arkfbp-browser",
  "version": "0.0.1",
  "description": "ark flow based programming browser version",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "LGPL",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check",
    "prepublish": "tsc"
  },
  "devDependencies": {
    "gts": "^1.1.2",
    "typescript": "~3.7.0",
    "@types/node": "^10.0.3"
  },
  "dependencies": {
    "@types/debug": "^4.1.5",
    "axios": "^0.19.0",
    "debug": "^4.1.1"
  }
}
